module.exports = {
    checkWin: (card) => {
        let winStatus = false
        // const row = card.square.
        return winStatus;
    },
    checkWinRows: (card) => {
        // condition: squares where row = 1, 2, 3, 4, or 5, && every(square => square.status === true);
    }
} 